import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { AproximitePage } from './aproximite';

@NgModule({
  declarations: [
    AproximitePage,
  ],
  imports: [
    IonicPageModule.forChild(AproximitePage),
  ],
})
export class AproximitePageModule {}
